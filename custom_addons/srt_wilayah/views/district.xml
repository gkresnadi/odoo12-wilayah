<odoo>
  <data>
    <!-- explicit list view definition -->
    <record model="ir.ui.view" id="srt_wilayah.district_list">
      <field name="name">Kecamatan</field>
      <field name="model">srt_wilayah.district</field>
      <field name="arch" type="xml">
        <tree>
          <field name="dist_id"/>
          <field name="reg_id"/>
          <field name="name"/>
        </tree>
      </field>
    </record>

    <!-- actions opening views on models -->
    <record model="ir.actions.act_window" id="srt_wilayah.action_district_window">
      <field name="name">Kecamatan</field>
      <field name="res_model">srt_wilayah.district</field>
      <field name="view_mode">tree,form</field>
    </record>
  </data>
</odoo>